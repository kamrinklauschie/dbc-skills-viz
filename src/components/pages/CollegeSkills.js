import React, { PureComponent } from 'react';
import ExpandableTree from '../ExpandableTree';

class CollegeSkills extends PureComponent {
  render() {
    return (
      <ExpandableTree />
    );
  }
}

export default CollegeSkills;
